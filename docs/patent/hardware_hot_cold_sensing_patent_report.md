# 硬體資料溫度感知與記憶體層級放置控制

Hardware Data Temperature Sensing and Memory Tier Placement Control

**文件類型**：概念報告（專利正式版前身）
**建立日期**：2026-04-08
**文件狀態**：初稿 — 背景、問題定義、解決方向

---

## 一、背景

### 1.1 異質記憶體架構的興起

現代資料處理系統——包含 AI 加速器、高效能運算系統、資料中心處理器——普遍採用異質記憶體架構。系統中同時存在多種記憶體層級，各層級在存取延遲、頻寬、容量等特性上差異顯著：

- **近端記憶體（near memory）**：與運算單元同封裝，延遲低、頻寬高，但容量有限。典型代表為 HBM（High Bandwidth Memory），以 3D 堆疊方式與處理器晶片共同封裝，提供數 TB/s 等級的頻寬。
- **遠端記憶體（far memory）**：經由晶片間互連或擴展介面存取，延遲較高，但容量大幅提升。包括 Host DDR DRAM（透過傳統記憶體通道連接）、CXL-attached DRAM（透過 CXL 協定擴展的外接記憶體），以及 LPDDR（經由 interposer 或 bridge 連接）。

此外，在多運算單元的 SoC 中，不同運算單元到不同記憶體的拓撲距離不等（NUMA 特性），使得「同一筆資料放在不同記憶體位置」對效能的影響更加不均。

### 1.2 Cache Mode 與 Flat Mode

在異質記憶體架構中，近端記憶體與遠端記憶體之間的關係存在兩種根本不同的組織方式：**cache mode** 與 **flat mode**。理解這兩種模式的差異，是理解本案技術貢獻的前提。

**Cache mode** 是指將近端記憶體（如 HBM）當作遠端記憶體（如 DDR）的最後一級快取（last-level cache）。在此模式下，HBM 不具備獨立的位址空間，軟體看到的只有 DDR 的位址。當處理器存取資料時，硬體自動檢查資料是否已快取在 HBM 中——若命中則直接回傳，若未命中則從 DDR 載入至 HBM，並在 HBM 空間不足時以固定的替換策略（如 LRU）自動驅逐舊資料。整個過程對軟體完全透明，無需任何資料放置決策。早期的異質記憶體系統（例如 Intel Knights Landing 處理器的 MCDRAM cache mode）即採用此設計。

**Flat mode** 則是將近端記憶體與遠端記憶體視為獨立的、各自可定址的記憶體空間。HBM 與 DDR 各自擁有獨立的位址範圍，軟體可以明確指定每一筆資料應存放在哪個記憶體層級。這意味著資料放置的決策權從硬體轉移到了軟體——硬體不再自動搬遷資料，而是由軟體（或輔助硬體機制）根據資料的存取行為主動決定放置位置。

當前業界主流的 AI 加速器——如 NVIDIA H100、AMD MI300X——均採用 flat mode。CXL-attached memory 從協定本質上即為 flat mode，因為 CXL 記憶體裝置在系統中呈現為獨立的位址區間。可以說，**flat mode 已成為高效能異質記憶體架構的主流選擇**。

業界從 cache mode 轉向 flat mode，有以下幾個關鍵原因：

1. **容量損耗**：Cache mode 需要為每個快取行（cacheline）維護 tag 元資料（用於記錄該行對應的 DDR 位址）。當 HBM 容量達到數十 GB 時，tag 所佔的儲存空間已不可忽略，相當於浪費了寶貴的近端記憶體容量。
2. **Cacheline 粒度引發頻繁顛簸（thrashing）**：Cache mode 以 cacheline（通常 64 bytes）為搬遷單位。當工作集（working set）略微超過 HBM 容量時，資料會在 HBM 與 DDR 之間以極高頻率來回搬遷，造成嚴重的效能下降。AI workload 中常見的情境——如 MoE 模型的 expert 切換——恰好容易觸發此問題。
3. **缺乏預測能力**：Cache mode 的替換策略僅依據過去的存取歷史（如 LRU）做被動決策，無法預測哪些資料即將被存取。對於具有明顯階段性存取模式的 AI workload（如依序處理模型各層的 weights），這種被動策略無法提前備妥所需資料。
4. **粒度不匹配**：AI workload 的資料搬遷需求通常以 tensor 或 expert 為單位（數十 KB 至數百 MB），而非 cacheline 的 64 bytes。Cache mode 的細粒度搬遷在此場景下既低效又缺乏語義。

### 1.3 資料溫度的本質

在 flat mode 架構中，資料放置決策由軟體負責，因此系統需要一種機制來判斷各記憶體區域的存取活躍程度。**並非所有資料在任何時刻都同等重要**。以 AI workload 為例：

- 正在被運算單元使用的 layer weights，需要最低延遲存取
- 數秒前已計算完畢的 layer weights，短期內不再被使用
- MoE 模型中當前 token 啟用的 expert，正在被密集存取
- MoE 模型中未被啟用的 expert，閒置中

我們以**溫度（temperature）**來描述資料的存取活躍程度。正在被頻繁存取的資料具有較高的**溫度分數（temperature score）**，稱為熱資料；不常被存取的資料溫度分數較低，稱為冷資料。當某筆資料的存取模式發生顯著改變——例如從冷變熱——即構成一個**溫度轉換事件（temperature transition event）**。理想情況下，熱資料應放置在近端記憶體（HBM）以獲得最低延遲，冷資料可放置在遠端記憶體（DDR 或 CXL memory）以釋放近端容量。

### 1.4 Flat Mode 下的管理空白與本案邊界

在晶片內部的 cache hierarchy 中（L1、L2、L3 等），硬體以固定的替換策略（如 LRU）自主管理資料的留存與驅逐。雖然部分架構提供有限的軟體提示機制（如 ARM 的 PLD prefetch 指令、x86 的 PREFETCHNTA），但 cache hierarchy 的核心定位仍是對軟體透明的自動管理，並不提供跨記憶體層級的主動感知與預測能力。

在 cache hierarchy 以外、flat mode 下的多層級 DRAM 記憶體之間（HBM、Host DDR、CXL memory），目前缺乏對等的硬體管理機制。這些記憶體層級之間的資料放置決策，仍仰賴作業系統軟體以毫秒級的輪詢方式執行，或由程式設計師靜態指定，不具備主動感知與預測能力。

**本案的涵蓋範圍**：本案聚焦於 flat mode 下的 DRAM 等級記憶體層級放置問題，涵蓋 HBM、Host DDR DRAM、CXL-attached DRAM 之間的資料搬遷決策。本案不涉及 cache hierarchy 內部的管理（L1/L2/L3 快取、on-chip SRAM、scratchpad memory），也不涉及 HBM 以 cache mode 運作時的資料管理——因為 cache mode 下的資料放置已由硬體快取機制自動處理，不需要額外的溫度感知。

這個 flat mode 下的管理空白，正是本案要填補的。

---

## 二、要解決什麼問題

### 2.1 問題的精確描述

在具有異質記憶體架構的資料處理系統中，運算所需的資料總量往往超過近端記憶體的容量。以 AI 加速器為例，大型語言模型的 weights 可達數百 GB，而單一加速器的 HBM 容量通常在 80 至 192 GB 之間，不足以容納全部資料。因此，在 flat mode（HBM 與 DDR 為獨立可定址記憶體空間，軟體決定資料放置）的系統組態下，資料需分佈在不同 DRAM-class 記憶體層級中——近端（HBM）、遠端（Host DDR）、擴展（CXL memory）——並在運算過程中動態搬遷：將即將被使用的資料搬入近端記憶體，將不再被使用的資料搬出至遠端記憶體。

此搬遷決策的核心挑戰在於：**如何即時判定各記憶體區域的溫度狀態，並在適當時機觸發搬遷。** 放置決策的品質直接影響效能——高溫資料放在近端記憶體可獲得低延遲存取，若誤留在遠端記憶體則需承受數倍的存取延遲。

問題的時間尺度差距尤為關鍵。以 Mixture-of-Experts（MoE）推論為例，每個 token 由 gating network 動態選擇少數 expert 執行運算，expert 的存取模式在毫秒內即可發生劇變。研究顯示，在 MoE 推論中 expert loading 佔推論時間的 85%–95%（HOBBIT, 2024），這意味著 expert 搬遷效率幾乎直接決定端到端推論速度。然而，現有搬遷決策機制的感知週期在秒級甚至十秒級，與 AI workload 毫秒級的存取模式變化之間存在數個數量級的落差。

### 2.2 現有搬遷決策機制及其限制

以下彙整六類現有機制，涵蓋從純軟體到硬體輔助的各種做法，逐一分析其運作方式與限制：

| 類別 | 代表技術 | 做法 | 限制 |
|------|---------|------|------|
| 軟體靜態配置 | numactl、membind API | 程式設計師或部署工具在啟動時指定資料的記憶體放置位置，運行期間不變 | 純靜態，無法適應運行期間動態變化的存取模式；需要程式設計師對 workload 有完整先驗知識 |
| Fault 驅動遷移 | CUDA Unified Memory | GPU MMU 偵測到 page 不在本端記憶體時觸發 page fault，由 driver 將 page 從遠端搬入近端 | 完全被動——必須等到實際存取 miss 發生才觸發搬遷，搬遷期間運算單元 stall；無預測能力，每次首次存取都付出 fault 代價 |
| 作業系統掃描 | Linux AutoNUMA、TPP（Transparent Page Placement） | 定期將部分頁面的 PTE access bit 清除或標記為 not-present，藉由後續存取觸發的 page fault 或下次掃描時的 access bit 狀態判斷冷熱，統計後決定搬遷 | 掃描週期典型為 10–60 秒，對 AI workload 毫秒級的存取模式變化而言反應太遲；掃描本身消耗 CPU 資源，且頁面標記清除引發額外 TLB shootdown 開銷 |
| HBM cache mode | 部分處理器架構之 HBM cache 組態 | HBM 作為 DDR 的 last-level cache，以 cacheline 粒度（64B）由硬體自動管理資料留存 | cacheline 粒度（64B）導致 tag 儲存開銷佔 HBM 容量的 3%–12%，浪費寶貴的高頻寬記憶體；當 working set 略微超過有效 cache 容量時易發生 thrashing；硬體替換策略固定（如 LRU），無預測能力，軟體無法調整判定標準 |
| GPU access counter | NVIDIA Volta 及後續架構之 access counter（US10,361,722） | 在 GPU 端為每個 page 維護硬體存取計數器，當計數超過可配置閾值時向 driver 發送通知，driver 決定是否遷移 page | 僅計數單一端點的存取，無跨裝置全域視野；基於累積計數的閾值觸發，無法區分存取密度的時間變化；不具備預測能力——只能回報已發生的存取，無法預判即將被存取的區域 |
| CXL 3.1 CHMU | CXL 3.1 規範定義之 Component Hot-tracking and Monitoring Unit | CXL memory device 內部追蹤各 region 的存取熱度，維護 hotlist ring buffer，以 interrupt 或 polling 方式通知 host；內部追蹤演算法為 implementation-defined | 僅限裝置端視角，無法觀察其他記憶體層級的存取狀況，缺乏跨裝置全域視野；內部演算法為各實作自行定義，host 無法調整或統一管理；不具備預測能力——僅回報當前已觀察到的熱區域 |

**六類機制的共同根本限制：** 上述所有機制——無論是軟體掃描、fault 驅動、硬體計數器、或裝置端熱度追蹤——皆只能回報**當前已被存取**的記憶體區域。沒有任何一種機制能夠在資料**尚未被存取之前**，預判其即將變熱並提前通知系統準備搬遷。這是現有技術的根本性缺口：從「感知」到「決策」之間缺乏預測維度，所有搬遷都是事後反應，而非事前準備。

### 2.3 適用場景與不適用場景

本案所針對的問題並非適用於所有記憶體管理情境。以下明確界定本案具優勢的場景，以及現有技術已足夠處理、不需要本案的場景。

**適用場景：存取模式在運行期間動態變化，且存在可學習的統計規律**

| 場景 | 為何適用 | 適用強度 |
|------|---------|---------|
| MoE expert routing | 每個 token 由 gating network 動態選擇少數 expert，被選中的 expert 在各 token 間不斷切換；gating 決策不具跨 token 記憶，無法由前一 token 的選擇直接推導下一 token 的選擇。然而 expert 的啟用在統計上存在可觀察的規律——特定 expert 組合傾向共同出現，且啟用頻率隨輸入分佈呈現可學習的模式 | 高 |
| 推薦系統 embedding table | 每次請求動態查詢不同的 embedding entry，熱門 entry 集合隨趨勢話題、時段漂移；不同 entry 之間存在共現模式（co-occurrence pattern），可從歷史存取序列中學習 | 高 |
| 多模型服務（multi-model serving） | 流量動態決定載入哪個模型，各模型的 weights 需在記憶體層級間搬遷；模型載入需求隨時段（time-of-day）呈現可學習的週期性模式 | 中 |

上述場景的共同特徵：存取模式在運行期間以毫秒至秒級變化，無法在 compile-time 預知，但變化並非完全隨機——存在可從歷史溫度轉換事件（temperature transition event）序列中學習的統計規律。

**不適用場景：存取模式可在 compile-time 預知，或變化速度在現有機制的感知範圍內**

- **LLM layer-by-layer 推論**：Transformer 模型的各 layer 按固定順序依次執行，存取順序在 compile-time 即可完全確定。軟體排程器（如 FlexGen）可預先規劃每一層的資料搬遷時機，不需要運行期間的動態感知與預測。
- **KV cache 管理**：KV cache 隨 sequence length 線性增長，增長模式完全可預測，不存在需要學習的動態模式。記憶體配置可由軟體依 sequence 長度預先規劃。
- **通用伺服器 NUMA 記憶體管理**：一般伺服器 workload 的記憶體存取模式變化速度在秒級，作業系統毫秒級的掃描機制（如 AutoNUMA）已足以應對，不需要更精細的硬體感知與預測。

### 2.4 預計解決方向

本案提出一種資料溫度感知方法，適用於 flat mode 下 DRAM-class 記憶體層級之間的搬遷決策輔助。此方法以多維度存取行為評估產生各記憶體區域之溫度分數（temperature score），並從溫度轉換事件（temperature transition event）的序列中自主學習規律以預測後續轉換，產生放置建議（placement hint）。其目標在於填補現有技術的根本性缺口——從僅能回報「已發生之存取」的被動感知，進化為能夠預判「即將發生之存取」的主動預測，使搬遷決策得以在資料被實際存取之前提前準備。

---

## 三、解決方法

在具有異質記憶體架構的資料處理系統中，各記憶體區域的存取活躍度隨運算階段持續變化。部分區域正被密集存取，部分區域已不再被使用但仍佔據近端記憶體空間，另有部分區域即將被存取但尚未被搬入。這些區域共存於容量有限的近端記憶體中，如何即時且準確地判定各區域的冷熱狀態，並在適當時機觸發搬遷，直接決定系統效能。

習知做法中，冷熱判定依賴作業系統以毫秒級輪詢掃描，或由記憶體裝置端獨立計數後回報——前者反應遲緩，後者僅看到各自端的局部流量，兩者皆不具備預測未來冷熱變化的能力。

有鑑於此，本案提出一種架構與方法，使硬體於記憶體存取過程中，依據各區域之存取行為自主評估冷熱狀態，無需軟體持續介入。

- 感知單元設置於互連的流量匯聚點，以旁路窺探方式觀察全域存取流量，由硬體即時產生放置建議
- 感知邏輯與互連資料路徑解耦，不受底層互連架構限制，適用於各種匯流排拓撲
- 於大量異質資料動態競爭有限近端記憶體之場景，在不增加記憶體容量之前提下，同時改善放置即時性與預測準確度

### 3.1 核心概念

選擇互連的流量匯聚點作為感知位置，是因為它是所有記憶體存取請求的必經之路。無論哪個運算單元存取哪個記憶體，請求都會通過互連。在記憶體裝置端觀察只能看到自己那一端的流量，在個別運算單元端觀察只能看到自己發出的請求，唯有在互連的匯聚點才能看到完整的全域畫面。

### 3.2 冷熱感知計算單元

本案的核心包含一個**冷熱感知計算單元**與一個**冷熱判定單元**。冷熱感知計算單元整合時間、空間、預測性三種策略，所有路徑共用一張區域狀態表並合成單一冷熱分數。冷熱判定單元負責監控分數變動、偵測閾值跨越事件、產生 placement hint 並觸發中斷通知。

**整體運作流程**：

當一筆記憶體存取請求通過晶片內互連時，旁路窺探電路擷取其位址資訊並轉換為 region 識別碼，送入冷熱感知計算單元。時間策略對該 region 在區域狀態表中的分數加分；同時，感知單元以可配置的週期對區域狀態表中所有 region 的分數執行右移（right shift），等效於將分數減半，使未被持續存取的 region 分數逐步歸零。當某個 region 的分數從下方跨越 promote 閾值（冷轉熱）時，此事件同時觸發空間策略與預測性策略——空間策略對該 region 在位址空間中相鄰的 region 加分；預測性策略向行為學習表查詢已學習到的後繼 region 並對其加分。三種策略的加分結果都寫回同一張區域狀態表。冷熱判定單元持續監控分數變動，當任何 region 的分數跨越提升或降級閾值時，產生 placement hint 並透過 IRQ 通知處理器。處理器讀取 CSR 中的 hint 資訊，決定並執行資料搬遷。

**三種策略各自的角色**：

**時間策略（持續性）**：每次窺探到存取請求時，對該 region 在區域狀態表中的分數加分。感知單元以可配置的週期對所有 region 的分數執行右移（等效於分數減半），未被持續存取的 region 分數因此逐步歸零。此策略持續追蹤各 region 的存取活躍度。

**空間策略（事件觸發）**：當某個 region 從冷轉熱時，對位址空間中相鄰的 region 加分。此策略僅在冷轉熱方向觸發——冷卻不需要介入，因為週期性右移已自然處理。

**預測性策略（事件觸發）**：當某個 region 從冷轉熱時，查詢行為學習表，若命中已確認的序列規律，對預測的後繼 region 加分。

**行為學習表的自主學習**：

行為學習表自主觀察冷熱轉換的先後順序——當某個 region 冷轉熱後，記錄其與下一個冷轉熱 region 的對應關係。重複出現的對應關係累積為已確認的規律，長期未再出現的對應關係自動失效。當新的冷轉熱事件發生時，若該 region 已有已確認的後繼規律，輸出對後繼 region 的預測。

**行為學習表的基本結構**：

行為學習表由有限數量的 entry 組成，每個 entry 記錄一組前驅/後繼對應關係。每個 entry 至少包含：前驅 region 識別碼（predecessor）、後繼 region 識別碼（successor）、以及信心度計數器（confidence counter）。後繼的判定以下一個冷轉熱事件所對應的 region 為準。每次觀察到相同的前驅/後繼對應關係時，信心度計數器累加；當信心度超過可配置閾值時，該規律被視為已確認。一個前驅 region 可對應多個後繼 entry（不同 successor），預測輸出時以信心度最高的 entry 優先。

表容量有限，當所有 entry 已滿時，以信心度最低者優先替換。失效機制方面，信心度計數器可透過週期性遞減（類似分數的右移衰減），使長期未被觀測到的對應關係信心度歸零後被自動回收。

**設計哲學**：三種策略不是獨立電路的拼接，而是統一在「冷熱感知計算單元」中運作。所有路徑最終都對區域狀態表的分數加分，差別只在觸發條件與目標 region。

**硬體架構設計要點**：

- **區域粒度可配置**：每個被追蹤的 region 的大小由軟體透過 CSR 配置（例如 64KB、2MB 等）。區域狀態表的容量有限，當被存取的 region 數超過表容量時，以替換策略（如信心度最低者優先替換）回收 entry。
- **狀態表分組存取**：三種策略與週期性右移共四個寫入來源需存取同一張區域狀態表。狀態表可採用分組（banking）結構——依 region 識別碼的低位分組，使不同 region 的寫入得以並行，避免 port contention。週期性右移 FSM 按組順序掃描，不與同組的策略寫入衝突。
- **雙閾值遲滯判定**：冷熱判定採用 promote 閾值與 demote 閾值分離的遲滯機制。分數從下方跨越 promote 閾值時觸發提升，從上方跌破 demote 閾值時才觸發降級。兩閾值之間的中間區維持現狀，避免週期性右移造成分數在閾值附近反覆震盪。

```
  score
    ▲
    │
  P ┤─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  promote 閾值
    │         ↑ 跨越：觸發 promote
    │
    │    （中間區：維持現狀，不觸發任何 hint）
    │
    │         ↓ 跌破：觸發 demote
  D ┤─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  demote 閾值
    │
    └──────────────────────────► time
```

**感知單元 Block Diagram**

```
                    互連 request channel
                            │
                            ▼
                  ┌────────────────┐
                  │  旁路窺探電路   │
                  └────────┬───────┘
                           │
                           ▼
  ╔══════════════════════════════════════════════════════════════════╗
  ║                    冷熱感知計算單元                                ║
  ║                                                                  ║
  ║   ┌────────────────────────────────────────────────────────────┐ ║
  ║   │                       區域狀態表                            │ ║
  ║   └────▲──────────────────▲──────────────────▲────────────────┘ ║
  ║        │                  │                  │                  ║
  ║        │  時間加分          │  空間加分         │  預測加分          ║
  ║        │                  │                  │                  ║
  ║   ┌────┴─────┐        ┌───┴─────┐        ┌───┴───────────┐    ║
  ║   │ 時間策略  │        │ 空間策略 │        │  預測性策略    │    ║
  ║   │          │        │         │        │               │    ║
  ║   │          │        │         │        │ ┌───────────┐ │    ║
  ║   │          │        │         │        │ │ 行為學習表 │ │    ║
  ║   │          │        │         │        │ └───────────┘ │    ║
  ║   └──────────┘        └────▲────┘        └──────▲────────┘    ║
  ║                            │                    │              ║
  ╚═══════════════════════════╤│════════════════════│══════════════╝
                              ││                    │
                              │▼ region 分數變動     │
                    ┌─────────┴──────────┐          │
                    │    冷熱判定單元      │          │
                    │  (閾值比較 + hint)  │          │
                    └──┬─────────────┬───┘          │
                       │             │  冷轉熱事件    │
                       │             └───────┴──────┘
                       │               (分發給空間策略
                       │                與預測性策略)
                       │
                       │ placement hint + 中斷通知
                                 ▼
                          處理器讀取 CSR
                          決定並執行搬遷
```

**各區塊職責**：

| 區塊 | 職責 |
|------|------|
| 旁路窺探電路 | 從晶片內互連的 request channel 擷取存取請求資訊，不參與 handshake 也不影響資料路徑 |
| 區域狀態表 | 為每個被追蹤的 region 維護一個冷熱分數，三種策略對同一張表加分；容量有限，以替換策略管理；可採用分組（banking）結構支援多來源並行存取 |
| 時間策略 | 收到存取資訊時對該 region 加分；以可配置的週期對所有 region 的分數執行右移（分數減半） |
| 空間策略 | 收到冷轉熱事件時，對位址空間中相鄰的 region 加分 |
| 預測性策略 | 收到冷轉熱事件時，向行為學習表查詢後繼 region 並對其加分 |
| 行為學習表 | 自主觀察冷熱轉換的先後順序，累積為已確認的規律，長期未出現則自動失效 |
| 冷熱判定單元 | 以雙閾值遲滯機制監控區域狀態表的分數變動——分數從下方跨越 promote 閾值時觸發冷轉熱事件（同時分發給空間策略與預測性策略），從上方跌破 demote 閾值時觸發降級，兩閾值之間維持現狀；產生 placement hint 並透過中斷通知處理器 |

### 3.3 系統架構

```
┌────────────────────────────────────────────────────────────────────────────┐
│                              資料處理系統                                    │
│                                                                            │
│  ┌────────┐ ┌────────┐     ┌────────┐  ┌───────────┐  ┌───────────┐      │
│  │ 處理   │ │ 處理   │ ··· │ 處理   │  │  處理器   │  │   DMA     │      │
│  │ 元件 0 │ │ 元件 1 │     │ 元件 N │  │ (CPU/FW)  │  │   引擎    │      │
│  └───┬────┘ └───┬────┘     └───┬────┘  └─────┬─────┘  └─────┬─────┘      │
│      │          │               │             │ ▲            │             │
│  ════╪══════════╪═══════════════╪═════════════╪═╪════════════╪══════════   │
│  ║                  晶片內互連                 │            │           ║  │
│  ║                                        IRQ │            │           ║  │
│  ║  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓  │            │           ║  │
│  ║  ┃     資料溫度感知單元 【本案】         ┃──┘            │           ║  │
│  ║  ┃                                    ┃               │           ║  │
│  ║  ┃  旁路窺探   區域狀態表     冷熱判定 ┃ CSR           │           ║  │
│  ║  ┃  存取請求   (冷熱感知計算單元) 單元  ┃◁────┘           │           ║  │
│  ║  ┃                                    ┃                │           ║  │
│  ║  ┃  對互連資料路徑零影響               ┃                │           ║  │
│  ║  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛                │           ║  │
│  ║                                                         │           ║  │
│  ═══╤════════════════╤════════════════╤════════════════════╤════════    │
│     │                │                │                    │           │
│  ┌──┴──────┐     ┌──┴──────┐     ┌──┴──────┐       ┌────┴─────┐     │
│  │近端記憶體│     │近端記憶體│     │遠端記憶體│       │ CXL/PCIe │     │
│  │ 控制器  │     │ 控制器  │     │ 控制器  │       │ 控制器   │     │
│  └──┬──────┘     └──┬──────┘     └──┬──────┘       └────┬─────┘     │
│     │                │               │                   │           │
└─────┼────────────────┼───────────────┼───────────────────┼───────────┘
      │                │               │                   │
   ┌──┴──┐         ┌──┴──┐        ┌──┴──────┐      ┌─────┴─────┐
   │近端 │         │近端 │        │ 遠端    │      │  擴展     │
   │記憶體│         │記憶體│        │ 記憶體  │      │  記憶體   │
   └─────┘         └─────┘        └─────────┘      └───────────┘
   (HBM)           (eDRAM,        (Host DRAM)      (CXL memory)
                    scratchpad)
```

**分散式部署**：上圖為集中式互連（crossbar / shared bus）的部署方式。在分散式互連拓撲（如 mesh NoC）中，單一節點無法觀察全域流量。此時可於各記憶體控制器域（MC domain）的匯聚節點各部署一個感知單元。由於所有對某位址範圍的存取最終都會匯聚至負責該範圍的記憶體控制器，在其前方設置感知單元即可觀察該位址範圍的完整流量。

在基本模式中，各感知單元獨立運作，空間策略與預測性策略的效果限於各感知單元所負責之位址範圍內。跨位址範圍的序列規律不在此模式的覆蓋範圍內。

在一替代實施例中，各感知單元可透過輕量級事件通知（僅傳遞 region ID + 冷轉熱標記）將冷轉熱事件傳遞至鄰近感知單元，使行為學習表可記錄跨位址範圍的序列規律。此通知不傳遞完整分數資訊，僅傳遞事件觸發信號。

```
  ┌────────┐  ┌────────┐          ┌────────┐  ┌────────┐
  │ 處理   │  │ 處理   │          │ 處理   │  │ 處理   │
  │ 元件 0 │  │ 元件 1 │          │ 元件 2 │  │ 元件 3 │
  └───┬────┘  └───┬────┘          └───┬────┘  └───┬────┘
      │           │                    │           │
  ════╪═══════════╪════════════════════╪═══════════╪═══════════
  ║           MC Domain 0           │         MC Domain 1      ║
  ║                                 │                          ║
  ║   ┏━━━━━━━━━━━━━━━━━━━━━┓      │   ┏━━━━━━━━━━━━━━━━━━━┓  ║
  ║   ┃     感知單元 0       ┃      │   ┃     感知單元 1     ┃  ║
  ║   ┃  (負責位址範圍 A)    ┃      │   ┃  (負責位址範圍 B)  ┃  ║
  ║   ┗━━━━━━━━━┯━━━━━━━━━━━┛      │   ┗━━━━━━━━┯━━━━━━━━━━┛  ║
  ║             │                  │            │              ║
  ══════════════╪══════════════════╪════════════╪═══════════════
                │                  │            │
           ┌────┴────┐        ┌───┴───┐   ┌────┴────┐
           │  MC 0   │        │ MC 1  │   │  MC 2   │
           └────┬────┘        └───┬───┘   └────┬────┘
                │                 │             │
           ┌────┴────┐       ┌───┴───┐    ┌────┴────┐
           │ 近端    │       │ 近端  │    │ 遠端    │
           │ 記憶體 0 │       │記憶體1│    │ 記憶體  │
           └─────────┘       └───────┘    └─────────┘
```

### 3.4 軟硬體協作

感知單元與處理器是 cowork 的關係。感知單元透過 IRQ 通知處理器有新的 hint，處理器透過 CSR 讀取 hint 並配置評估參數。搬遷的決策與執行由處理器透過既有的 DMA 機制完成。

- **感知單元**：旁路窺探、基本操作（score 累加與週期性右移衰減）、冷轉熱延伸（空間 + 預測）、行為學習表 自主學習、hint 產生、IRQ 通知
- **處理器**：CSR 配置評估參數（右移衰減週期、閾值、各策略的修正強度等，可依位址區間分別設定）、讀取 hint、決定並執行搬遷
- **搬遷抑制**：感知單元為每個 region 提供搬遷進行中的抑制旗標（migration pending flag），由軟體在啟動搬遷時透過 CSR 設定、搬遷完成後清除。抑制期間不對該 region 產生新的 placement hint，避免重複搬遷

---

## 四、新穎進步簡述

### 習知 vs 本案對比

以 CXL pooled memory 場景為例，對比習知做法（CXL 3.2 CHMU，記憶體裝置端感知）與本案（互連 / switch 層級感知）的架構差異。同樣的對比邏輯亦適用於晶片內互連場景（見 3.3 節）。

**習知做法：CXL 3.2 CHMU — 各 Type 3 記憶體裝置端各自感知，回報 Host OS 決策**

```
  ┌────────┐       ┌────────┐
  │ Host 0 │       │ Host 1 │
  └───┬────┘       └───┬────┘
      │                │
  ════╪════════════════╪═══════════════════
  ║           CXL Switch / Fabric        ║
  ║     （僅路由轉發，不參與冷熱感知）     ║
  ═══╤═══════════════╤═══════════════╤════
     │               │               │
  ┌──┴──────────┐ ┌──┴──────────┐ ┌──┴──────────┐
  │CXL Type 3   │ │CXL Type 3   │ │CXL Type 3   │
  │ Mem Dev 0   │ │ Mem Dev 1   │ │ Mem Dev 2   │
  │             │ │             │ │             │
  │ ┌────────┐  │ │ ┌────────┐  │ │ ┌────────┐  │
  │ │ CHMU 0 │  │ │ │ CHMU 1 │  │ │ │ CHMU 2 │  │
  │ └────┬───┘  │ │ └────┬───┘  │ │ └────┬───┘  │
  └──────┼──────┘ └──────┼──────┘ └──────┼──────┘
         │               │               │
         │  各自回報      │  各自回報       │  各自回報
         └───────┬───────┴───────┬───────┘
                 │               │
                 ▼               ▼
            ┌─────────┐    ┌─────────┐
            │ Host 0  │    │ Host 1  │   Host OS 彙整各 CHMU
            │ OS 決策  │    │ OS 決策  │   的獨立報告後決策
            └────┬────┘    └────┬────┘   含 OS 彙整路徑延遲
                 │              │         跨 device 的空間/預測策略
                 ▼              ▼         由 spec 未規定（impl-defined）
               DMA 搬遷      DMA 搬遷
```

**CHMU 流程**：① 各 CXL Type 3 memory device 內的 CHMU 以有限數量的 counter 追蹤到達自己端的存取（追蹤本質為 imprecise；支援 epoch-based 與 always-on 兩種模式，可配置 DPA region filtering、R/W filtering 與 downsampling）→ ② counter 超過閾值的 region 被加入 PCI BAR 中的 hotlist ring buffer，軟體可以 polling 或 interrupt（watermark / overflow）方式讀取 → ③ Host OS 讀取並彙整多個 device 的 hotlist，決定搬遷 → ④ Host 透過 DMA 執行搬遷

**本案：互連 / Switch 層級感知，冷熱感知計算單元主動評估，硬體即時輸出 hint**

```
  ┌────────┐       ┌────────┐
  │ Host 0 │       │ Host 1 │
  └───┬────┘       └───┬────┘
      │                │
  ════╪════════════════╪═══════════════════════
  ║              CXL Switch / Fabric          ║
  ║                                           ║
  ║  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓    ║
  ║  ┃       資料溫度感知單元 【本案】     ┃────║──► IRQ
  ║  ┃                                   ┃    ║
  ║  ┃  旁路窺探所有 host 對所有 device   ┃ CSR║◁── Host
  ║  ┃  的 CXL.mem request               ┃    ║    配置
  ║  ┃                                   ┃    ║
  ║  ┃  時間策略 + 空間策略 + 預測性策略   ┃    ║
  ║  ┃  → 複合冷熱分數 → placement hint  ┃    ║
  ║  ┃                                   ┃    ║
  ║  ┃  全域可見：跨 host、跨 device      ┃    ║
  ║  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛    ║
  ║                                           ║
  ═══╤═══════════════╤═══════════════╤═════════
     │               │               │
  ┌──┴──────────┐ ┌──┴──────────┐ ┌──┴──────────┐
  │ CXL Mem 0   │ │ CXL Mem 1   │ │ CXL Mem 2   │
  └─────────────┘ └─────────────┘ └─────────────┘
```

**本案流程**：① 感知單元旁路窺探所有經過 switch 的 CXL.mem request（address + R/W type；在支援來源識別之互連實作中，可進一步擷取來源識別資訊）→ ② 三種策略即時更新區域狀態表中的冷熱分數 → ③ 分數跨越 promote/demote 閾值時，硬體產生 placement hint 並透過 IRQ 通知 Host → ④ Host 讀取 CSR 中的 hint，決定並執行搬遷

**關鍵差異**：

| 面向 | CHMU（習知） | 本案 |
|------|-------------|------|
| **感知位置** | 各 CXL Type 3 memory device 內部 | CXL switch / 互連的流量匯聚點 |
| **可見範圍** | 僅到達該 device 的 request | 所有 host 對所有 device 的 request |
| **跨 device 感知** | 不可能——Device 0 的 CHMU 看不到 Device 1 的流量 | 自然可見——switch 是所有流量的必經之路 |
| **空間策略** | CXL 3.2 spec 未規定空間策略；CHMU 內部演算法為 implementation defined | 有——跨 device 的空間擴散偵測 |
| **預測性策略** | CXL 3.2 spec 未規定預測性策略；CHMU 內部演算法為 implementation defined | 有——從跨 device 的冷熱轉換序列學習規律，主動預測 |
| **衰減機制** | Epoch-based 模式在 epoch 結束時 snapshot/reset counter；always-on 模式持續計數至超過 threshold | 所有 region 的分數持續性週期右移遞減，反映即時活躍度變化 |
| **多 host 聚合** | CHMU 位於 device 端，不區分 request 來源 | 在互連協定或實作支援來源識別之場景下，可進一步擷取來源識別資訊 |
| **決策延遲** | CHMU 回報（支援 polling 或 interrupt）→ OS 讀取彙整 → OS 決策 | 硬體即時產生 hint → 中斷通知 Host |
| **軟體負擔** | OS 需讀取並彙整多個 CHMU 的 hotlist、決策 | 硬體完成感知與評估，OS 只需回應 hint |

### 習知技術分類

| 類別 | 代表來源 | 做法 | 限制 |
|------|---------|------|------|
| OS fault-based 掃描 | Linux AutoNUMA、TPP (ASPLOS '23) | 定期將部分頁面 PTE 標記為 not-present，藉存取觸發的 page fault 判斷冷熱並決定搬遷 | 毫秒級輪詢，對 AI workload 反應太遲；消耗 CPU 資源 |
| 記憶體端硬體計數 | NeoProf (MICRO '24) | 記憶體裝置端統計各 page 存取資訊（頻率、頻寬、讀寫比等），回報 OS | 感知在 off-chip，只看到流向自己端的流量；不自主決策 |
| 軟體配置 / fault 驅動 | numactl、CUDA Unified Memory | numactl：程式設計師預先指定放置位置（靜態）；CUDA UM：GPU MMU 偵測到 page 不在本端時觸發 fault 驅動遷移（被動反應式） | 靜態配置無法適應動態存取模式；fault 驅動需等到實際存取 miss 才觸發，無預測能力 |
| OS 軟體 page metadata | Intel US12,443,537 | 在 kernel memory 中維護 page hotness metadata，以 callback 與 region filtering 降低偵測開銷 | OS 軟體層級方案；不含空間或預測性策略；不在晶片內互連中感知 |
| CXL 裝置端硬體監測 | CXL 3.2 CHMU (Dec 2024) | 各 CXL Type 3 memory device 內嵌 Hotness Monitoring Unit，支援 epoch-based 與 always-on 兩種模式，可配置 DPA region filtering、R/W filtering 與 downsampling；透過 PCI BAR hotlist ring buffer 回報（支援 polling / interrupt） | 各 device 獨立監測，僅看到流向自己端的流量；CXL 3.2 spec 未規定空間或預測性策略（內部演算法為 implementation defined）；host 需彙整多個 CHMU 報告；感知不在互連層級 |
| 存取頻率預測 | Huawei US12,379,861 | 根據區塊鄰接關係以預測模型推估存取頻率 | Storage 語境；非晶片內互連層級；非硬體狀態表的序列規律學習 |
| 純軟體 tiering | Chrono (EuroSys '25) | 以 timer 精確測量 page access frequency，自動調參進行冷熱分類 | 不在硬體層執行，反應速度受限於軟體路徑延遲 |
| 推理/訓練協同調度 | Sirius (USENIX ATC '25) | runtime 軟體層 selective eviction：profiling hot/cold 訓練資料，inference burst 來時將 cold pages 搬至 host memory | 純軟體方案，需 runtime instrumentation；以軟體啟發式做 eviction 決策；不含序列規律預測；針對 GPU 場景而非通用互連層級 |
| 延遲感知 tiering | Colloid (SOSP '24) | 基於各 tier 的實測存取延遲（loaded latency）在 tiers 間平衡熱資料分布 | 不在硬體層執行；依賴處理器端的延遲測量（如 CHA） |
| Cache replacement | ARC (FAST '03)、LIRS (SIGMETRICS '02) | Cache 內部的替換決策演算法 | 僅處理 cacheline 粒度的 cache 內部管理，不涉及跨記憶體層級搬遷 |

### 新穎性

1. **互連 / 流量匯聚點的感知位置**：將冷熱感知點設置在互連的流量匯聚點（晶片內互連或 CXL switch），以旁路窺探方式擷取存取請求資訊。此位置能同時觀察所有來源對所有記憶體的存取流量。現有最接近的硬體方案（CXL 3.2 CHMU）將感知置於記憶體裝置端，各 device 僅看到流向自己的流量，無法觀察跨 device 的全域畫面

2. **冷熱感知計算單元的統一架構**：將時間、空間、預測性三種策略整合於同一個計算單元中——時間策略持續累加，空間策略在冷轉熱時對鄰近 region 累加，預測性策略在冷轉熱時查 行為學習表 對後繼 region 累加。三者寫入同一個 score 欄位，形成單一的複合冷熱評估。現有方案沒有將三者整合在同一架構中

3. **冷轉熱序列規律的硬體自主學習**：作為冷熱感知計算單元中預測性策略的核心，行為學習表自主觀察冷熱轉換的先後順序，重複出現的對應關係累積為已確認的規律，長期未再出現則自動失效。預測的是 region 的冷熱狀態轉換序列，而非位址的算術規律

### 進步性

1. **硬體速度的主動式感知**：感知單元持續評估所有被追蹤的 region，不需等待 miss 或 OS 輪詢觸發，反應延遲遠低於軟體方案的毫秒級

2. **全域可見性帶來更精確的決策**：在互連上觀察，能看到跨運算單元、跨記憶體層級的完整存取畫面，避免單一端點觀察的資訊不完整

3. **與現有記憶體管理機制互補**：感知單元只產生 placement hint，軟體決定是否及如何執行搬遷，不替代現有架構

---

## 五、專利構想摘要

### 核心概念

> 在資料處理系統的晶片內互連中設置一硬體感知單元，以旁路窺探方式擷取記憶體存取請求。感知單元的核心為一**冷熱感知計算單元**，整合時間、空間、預測性三種策略——時間策略對被存取的 region 執行 score 累加，並以可配置週期對所有分數執行右移衰減；空間策略在某 region 冷轉熱時對相鄰 region 加分；預測性策略在某 region 冷轉熱時查詢 行為學習表 對學習到的後繼 region 加分。三者寫入同一個 score 欄位，形成複合冷熱評估。行為學習表 在每次冷轉熱時自主學習前驅/後繼對應關係。分數跨越 promote 或 demote 閾值時產生 placement hint，透過 IRQ 通知處理器執行搬遷。所有評估參數由軟體透過 CSR 配置。

### 鎖定的核心特徵

| 核心特徵 | 說明 | 選擇理由 |
|---------|------|---------|
| **A. 互連內旁路窺探** | 在晶片內互連中以旁路方式擷取存取請求資訊，不攔截也不修改請求 | 感知位置與所有 prior art 不同；唯一能觀察全域存取流量的位置 |
| **B. 冷熱感知計算單元** | 整合時間、空間、預測性三種策略於同一計算單元，三者寫入同一張 區域狀態表，形成單一複合冷熱分數 | 統一的運算架構，非獨立策略拼接；現有方案最多僅涵蓋其中一種策略 |
| **C. 行為學習表硬體自主學習** | 自主觀察冷熱轉換的先後順序，重複出現的對應關係累積為已確認的規律，長期未再出現則自動失效 | 預測 region 的狀態轉換序列而非位址算術規律；所有已知 prior art 均不具備 |

### 架構特色

- **硬體自主感知**：從窺探到評估到預測到學習，全由硬體完成，不需處理器介入
- **評估參數由軟體配置**：右移衰減週期、閾值、各策略的修正強度等評估參數皆可透過 CSR 設定，可依位址區間分別配置
- **感知與搬遷解耦**：感知單元只產生 placement hint，不直接操作記憶體。軟體決定是否及如何執行搬遷
- **對互連資料路徑零影響**：旁路窺探不在資料傳輸路徑上引入額外的 protocol-level 延遲
- **雙閾值遲滯判定**：promote 閾值與 demote 閾值分離，中間區維持現狀，避免週期性右移造成分數在閾值附近反覆震盪
- **區域粒度可配置**：軟體可透過 CSR 設定區域大小，硬體以有限容量的狀態表追蹤，超出容量時以替換策略管理
- **分散式部署**：在分散式互連拓撲中，可於各記憶體控制器域的匯聚節點獨立部署感知單元，各自負責對應位址範圍

---

## 六、實施例及應用例

### 實施例 1：LLM 推理服務 + LoRA 微調共用加速器
*LLM Serving + LoRA Fine-tuning Co-location*

**場景**：當前 data center 最常見的 co-location pattern——LLaMA 推理服務（chatbot，延遲敏感）與 LoRA 微調（背景任務）跑在同一張加速器上。架構上，上排 PE 跑推理，下排 PE 跑微調，兩者透過共享 L2 Cache 競爭同一張 HBM。

```
                ┌─────────────── AI Accelerator Chip ──────────────┐
                │                                                  │
                │  ┌── PE row 0 ──┐  ← LLaMA Inference (chatbot)   │
                │  │ PE PE PE..PE │     • Model weights            │
                │  └──────┬───────┘     • KV Cache (per request)   │
                │         │                                        │
  ┌──────┐      │  ┌──────┴───────────────┐                        │  ┌─────┐
  │ Host │ PCIe │  │   L2 Cache (Shared)  │              Memory I/F│  │     │
  │ CPU  │◄────►│  └──────┬───────────────┘                ◄──────►│  │ HBM │
  └──┬───┘NVLink│         │                                        │  │     │
     │          │  ┌──────┴───────┐  ← LoRA Fine-tuning (背景)     │  └─────┘
  ┌──┴───┐      │  │ PE PE PE..PE │     • Base model weights       │
  │ Host │      │  └──────────────┘     • Gradient / Optimizer st. │
  │ DRAM │      │                       • Batch data (epoch 切換)  │
  └──────┘      │                                                  │
                │  ┌── Processing Element (zoom) ─────────┐         │
                │  │ L1 Cache / Scratchpad ─► Tensor Core │         │
                │  │                       ─► Vector Unit │         │
                │  │                       ─► SFU         │         │
                │  └──────────────────────────────────────┘         │
                └──────────────────────────────────────────────────┘
```

**問題**：LoRA 微調每個 step 大量讀寫 gradient / optimizer state，存取頻率遠高於推理服務的間歇性 request。LRU 只看頻率，會把 LoRA 的資料判為「最熱」，把 LLaMA 的 KV Cache 與 weights 擠出 HBM——推理延遲飆升。

```
  ┌─────────── 沒有本案：LRU 判斷錯誤 ───────────┐
  │ HBM 內容：                                    │
  │   ▓▓ LoRA gradient        ← 高頻寫入，最熱   │
  │   ▓▓ LoRA weights         ← 高頻讀取         │
  │   ▓▓ LoRA batch data      ← 高頻讀取         │
  │   ░░ LLaMA KV Cache       ← 被擠出 ✗         │
  │   ░░ LLaMA weights        ← 被擠出 ✗         │
  │                                              │
  │ → 推理延遲飆升                                │
  └──────────────────────────────────────────────┘

  ┌─────────── 有本案：依區間配置 ───────────────┐
  │ 感知單元 CSR：                                │
  │   推理位址區間：長衰減週期 + 低 promote 閾值  │
  │     → 持續存取即可維持分數                    │
  │   訓練位址區間：短衰減週期 + 高 promote 閾值  │
  │     → epoch 切換後快速歸零 → demote          │
  │                                              │
  │ HBM 內容：                                    │
  │   ░░ LLaMA KV Cache       ← 穩定留在 HBM ✓   │
  │   ░░ LLaMA weights        ← 穩定留在 HBM ✓   │
  │   ▓▓ LoRA 當前 epoch 資料 ← 動態調整          │
  │   ░░ (LoRA 上個 epoch 已自動 demote)          │
  │                                              │
  │ → 推理延遲不受訓練影響                        │
  └──────────────────────────────────────────────┘
```

**本案配置**：軟體透過 CSR 為推理與訓練的位址區間分別配置感知參數，硬體依區間自動套用。

**效果**：

- LLaMA 的 KV Cache 與 weights 穩定留在 HBM，推理延遲不受訓練影響
- LoRA 上個 epoch 的資料隨 epoch 切換自動被 demote，騰出空間給新 batch
- 兩個工作負載動態共用 HBM，無需軟體手動切割空間，亦無需 runtime instrumentation

---

### 實施例 2：MoE 推理
*Mixture-of-Experts Inference*

```
                       ┌────────┐
                       │ Output │
                       └────▲───┘
                            ⊕  ◄── Gating 權重加權求和
                ┌───────────┼───────────┬────────┐
            ┌───┴───┐   ┌───┴───┐   ┌───┴───┐  ┌─┴────┐
            │Expert1│   │Expert2│   │Expert3│..│ExpertN│
            │ ★啟用 │   │ 未啟用 │   │ ★啟用 │  │ 未啟用│
            └───▲───┘   └───────┘   └───▲───┘  └──────┘
                └───────────┬───────────┘
                       ┌────┴─────┐
                       │  Gating  │
                       │ Network  │
                       └────▲─────┘
                       ┌────┴─────┐
                       │ Input    │
                       └──────────┘
```

**場景**：MoE 模型每層數百個 expert，HBM 僅能容納其中一部分。每個 token 由 Gating Network 選中 2~4 個 expert（圖中粉色），其餘不參與此 token 計算。

**問題**：

1. **反應式延遲**：被選中的 expert 若不在 HBM，須先搬入才能計算
2. **無跨 token 記憶**：Gating Network 只看當前 token，無法利用「Expert 1 → Expert 3」這類序列規律

**本案運作**：

- 時間策略對被存取的 expert 加分 → 跨越 promote 閾值 → 冷熱判定單元觸發冷轉熱事件
- 行為學習表觀察「Expert 1 變熱 → Expert 3 變熱」的順序，重複出現後確認為規律
- 下次 Expert 1 又冷轉熱時，預測性策略立刻對 Expert 3 加分 → promote hint
- 軟體在 Expert 3 真正被 Gating Network 選中**之前**就背景搬入 HBM
- 長期未啟用的 expert 因週期性右移歸零 → demote hint

**效果**：

- 重複出現的 expert 啟用序列從**反應式搬遷**變為**主動預搬遷**
- Expert 在實際被選中時已在 HBM，運算單元零等待開始計算
- 適用任何 routing 行為有統計規律的 MoE 模型，無需為每種模型客製 prefetch 邏輯

---

### 實施例 3：Chiplet 多晶粒架構
*Multi-Chiplet SoC Architecture*

```
  ┌──────────┐  ┌──────────┐  ┌──────────┐
  │ Chiplet 0│  │ Chiplet 1│  │ Chiplet 2│
  └────┬─────┘  └────┬─────┘  └────┬─────┘
       │             │             │
  ═════╪═════════════╪═════════════╪═══════
  ║          晶粒間互連                  ║
  ║   ┏━━━━━━━━━━━━━━━━━━━━━━━━━┓     ║
  ║   ┃      感知單元             ┃     ║
  ║   ┃  Region X 聚合存取偵測   ┃     ║
  ║   ┃  + 主要來源識別           ┃     ║
  ║   ┗━━━━━━━━━━━━━━━━━━━━━━━━━┛     ║
  ═════╤═════════════╤═════════════╤═══════
       │             │             │
  ┌────┴────┐  ┌────┴────┐  ┌────┴────┐
  │近端     │  │近端     │  │近端     │
  │記憶體 0 │  │記憶體 1 │  │記憶體 2 │
  └─────────┘  └─────────┘  └─────────┘
```

**場景**：SoC 採用 chiplet 架構，每個 chiplet 各自連接近端記憶體，同時透過互連存取其他 chiplet 的記憶體（remote access）。

**問題**：某塊資料同時被多個 chiplet 存取形成跨晶粒熱點，但各 chiplet 的記憶體控制器只看到自己端的流量，無法察覺。

**本案運作**：

- 感知單元設置於晶粒間互連，觀察所有 chiplet 對所有記憶體的存取請求
- 時間策略累加聚合存取活動 → 分數上升
- 空間策略偵測鄰近 region 也有跨晶粒存取 → 加分修正
- 在互連協定提供來源識別資訊之場景下，hint 可附帶主要存取來源（如「主要被 Chiplet 1 存取」）
- 跨晶粒存取消失時，分數經週期性右移下降 → 觸發 demote hint

**效果**：

- 跨晶粒熱點被自動偵測，無需 OS 週期性 NUMA 掃描
- 軟體據主要存取來源決定搬遷目的地（搬至最頻繁存取的 chiplet 的近端記憶體）
- 存取模式改變後自動反應，無需等待下次 OS 掃描週期

---

## 七、前案技術檢索說明

以多組關鍵字組合對 USPTO 已授權專利進行全文搜尋，共檢視約 120 件專利。以下列出與本案最相關的五件前案及其與本案的差異。

| # | 前案 | 權利人 | 與本案的關聯 | 與本案的差異 |
|---|------|--------|-------------|-------------|
| 1 | US12,443,537 | Intel | OS 軟體層級的 page hotness metadata tracking，以 region filtering 與 callback 降低偵測開銷 | OS/kernel 軟體方案，非晶片內硬體感知；不含空間策略與預測性策略；不在晶片內互連中感知 |
| 2 | US12,159,055 | METISX (現 XCENA) | 記憶體裝置端 controller 的存取監測方法 | 感知在 device-side controller，非晶片內互連；未揭露三種策略整合 |
| 3 | US12,379,861 | Huawei | 根據區塊鄰接關係以預測模型推估存取頻率 | Storage 語境，非晶片內互連層級；非硬體狀態表的序列規律學習 |
| 4 | US12,174,742 | AMD | 以 LSTM RNN 模型預測記憶體存取行為 | ML-based 預測，需 training data 與推理運算；非硬體 pattern table 的序列規律學習 |
| 5 | US12,449,978 | Micron | 記憶體裝置端追蹤存取活動，host 端建構熱度圖（heatmap）並決策搬遷 | 追蹤在 off-chip 記憶體裝置端，評估與決策在 host 端軟體；不含空間策略與預測性策略 |

### 前案檢索 PE 整理

以本案兩個核心特徵（A：互連內旁路窺探、B：三種冷熱評估策略）對比威脅等級最高的三件前案：

| | 本案 | D1：Intel US12,443,537 | D2：Huawei US12,379,861 | D3：METISX (現 XCENA) US12,159,055 |
|---|------|------------------------|------------------------|------------------------|
| **特徵 A：感知位置** | 晶片內互連，以旁路窺探擷取存取請求，全域存取流量可見 | OS kernel 層級的 page metadata tracking，不在互連中感知 | 非晶片內互連；storage 語境 | 記憶體裝置端 controller；非晶片內互連層級 |
| **特徵 B-1：時間策略** | 持續追蹤存取活躍度，以可配置週期對所有分數執行右移衰減 | 有揭露 page hotness tracking，但為 OS 軟體層級，非硬體即時衰減 | 追蹤存取頻率，但無時間衰減機制的揭露 | 有揭露存取監測，具體策略待確認 |
| **特徵 B-2：空間策略** | 冷轉熱時對鄰近 region 加正向分數修正 | 未揭露空間策略 | 有揭露「基於相鄰區塊」的頻率預測，但為 storage 語境且非分數修正機制 | 未揭露空間策略 |
| **特徵 B-3：預測性策略** | 從冷熱轉換序列學習規律，經重複觀測確認後預測後繼 region | 未揭露預測性策略 | 有揭露存取頻率預測，但以預測模型實作，非硬體序列規律學習 | 未揭露預測性策略 |
| **三策略整合** | 三者共用一張狀態表，合成單一分數 | 未揭露多策略整合 | 未揭露多策略整合 | 未揭露多策略整合 |

**PE 小結**：

- **D1（Intel）**為 OS kernel 層級的 page hotness metadata tracking，與本案在冷熱偵測目的上有關聯，但為軟體方案，不含空間策略與預測性策略，感知不在互連內部，與本案的硬體即時感知架構有根本差異
- **D2（Huawei）**涵蓋空間鄰接關係的頻率預測，與本案在空間策略概念上有交集，但為 storage 語境且以預測模型實作，與本案的硬體分數修正機制不同
- **D3（METISX (現 XCENA)）**為記憶體裝置端 controller 的存取監測，感知位置在 device side 而非晶片內互連；目前未見三策略整合的揭露
- **三策略整合為單一分數的架構**在所有前案中均未出現

### 檢索結論

以目前對 USPTO 已授權專利之初步檢索結果，尚未發現單一來源直接揭露本案核心組合——在互連的流量匯聚點以旁路窺探方式感知存取流量，以時間、空間、預測性三種策略整合評估冷熱並產生 placement hint 供軟體執行搬遷。本檢索為初步性質，正式申請前應由專利代理人或檢索機構執行涵蓋 PCT、EP、CN、KR、JP 等管轄區的完整檢索。

---

## 八、Claim 規劃

### 裝置項

**1.** 一種資料溫度感知裝置，設置於一資料處理系統之晶片內互連中，該裝置包含：

一請求窺探電路，以旁路方式擷取通過該晶片內互連之記憶體存取請求的資訊；

一區域狀態表，為各記憶體區域維護一冷熱分數；

至少一存取行為追蹤電路，根據該請求窺探電路所擷取之資訊，以至少一種策略追蹤各記憶體區域之存取行為並更新該冷熱分數；

一判定電路，根據該冷熱分數與至少一可配置閾值之比較，產生放置建議；

其中，該請求窺探電路不攔截亦不修改該記憶體存取請求。

**2.** 如請求項 1 所述之裝置，其中該至少一存取行為追蹤電路包含一時間策略追蹤電路，持續追蹤各記憶體區域之存取活躍程度，並以一可配置週期對該區域狀態表中所有區域之冷熱分數執行右移操作，使未被持續存取之區域的冷熱分數逐步歸零。

**3.** 如請求項 1 所述之裝置，其中該至少一存取行為追蹤電路包含一空間策略電路，當一記憶體區域之冷熱分數從低於一閾值上升至高於該閾值時，對位址空間中相鄰之記憶體區域的冷熱分數加入正向修正。

**4.** 如請求項 1 所述之裝置，進一步包含一轉換序列規律預測電路，記錄冷熱轉換事件之前後區域對應關係，當該對應關係經重複觀測確認後，於偵測到已知前兆時對預測之後繼區域的冷熱分數加入修正。

**5.** 如請求項 1 所述之裝置，其中該至少一可配置閾值、右移衰減週期與修正量可由軟體透過組態暫存器設定，且配置可為全域預設或依位址區間分別設定。

**6.** 如請求項 1 所述之裝置，其中該晶片內互連為晶片內網路、交叉開關、或晶粒間互連之一。

**7.** 如請求項 1 所述之裝置，其中該資料處理系統包含一近端記憶體層級與一遠端記憶體層級，該放置建議指示各記憶體區域應被提升至該近端記憶體層級或降級至該遠端記憶體層級。

### 方法項

**8.** 一種資料溫度感知方法，於一資料處理系統之晶片內互連中執行，包含步驟：

以旁路方式擷取通過該晶片內互連之記憶體存取請求的資訊；

根據所擷取之資訊，以至少一種策略追蹤各記憶體區域之存取行為並更新一冷熱分數；

根據該冷熱分數與至少一可配置閾值之比較，產生放置建議；

其中，擷取步驟不攔截亦不修改該記憶體存取請求。

**9.** 如請求項 8 所述之方法，其中該至少一種策略包含一時間策略、一空間策略、及一預測性策略，三者更新同一冷熱分數。

**10.** 如請求項 9 所述之方法，其中該預測性策略包含記錄冷熱轉換事件之前後區域對應關係，經重複觀測確認後，於偵測到已知前兆時對預測之後繼區域的冷熱分數加入修正。

### 系統項

**11.** 一種資料處理系統，包含：

複數個處理元件；

一晶片內互連，連接該複數個處理元件；

一近端記憶體層級與一遠端記憶體層級；

一資料溫度感知裝置，設置於該晶片內互連中，包含一請求窺探電路、一區域狀態表、至少一存取行為追蹤電路、及一判定電路，該判定電路根據冷熱分數產生放置建議；

其中，軟體根據該放置建議決定是否執行該近端記憶體層級與該遠端記憶體層級之間的資料搬遷。

### 策略說明

| 決策 | 理由 |
|------|------|
| 獨立項使用「至少一種策略」 | 最小實作只需時間策略即可運作，保護範圍最寬 |
| 三種策略整合放在依附項（Claim 9） | 完整三策略組合作為 fallback 防線 |
| 預測性策略的序列規律學習放在依附項（Claim 4, 10） | 核心新穎性，必要時可併入獨立項強化 103 防禦 |
| 空間策略獨立為 Claim 3 | 與預測性策略分開保護，各自獨立 |
| CSR 可配置含位址區間粒度（Claim 5） | 涵蓋實施例 1 的多工作負載場景 |
| 互連類型不限定在獨立項 | Claim 6 依附項擴展至 NoC / crossbar / die-to-die |

---

## 九、§101 抽象概念風險評估（美國申請）

### 風險分析

在美國申請時，USPTO 審查委員可能依 35 U.S.C. §101（Alice/Mayo 二步測試）主張本案的核心概念——「追蹤存取行為並評估冷熱以產生放置建議」——屬於 abstract idea（如 mental process 或 mathematical concept）。

**Step 1（Abstract Idea 判定）**：Claim 1 獨立項描述的「窺探、追蹤、評估、產生建議」流程，若僅以功能性語言撰寫，可能被認定為可由人腦執行的 mental process。

**Step 2A Prong 2 + Step 2B（"Something More" 判定）**：需證明 claim 整體包含足以將抽象概念轉化為具體技術改進的額外要素。

### 防禦策略

| Claim | §101 防禦角色 | 關鍵要素 |
|:---:|---|---|
| **1** | 獨立項含「請求窺探電路」「區域狀態表」「存取行為追蹤電路」「判定電路」等具體硬體元件，且限定於「晶片內互連中」的物理位置 | 硬體裝置 + 物理位置限定 |
| **3** | **核心防禦項**：空間策略電路在冷轉熱事件發生時對鄰近區域加入分數修正，是即時回應硬體事件的具體電路行為，人腦無法在互連頻寬下即時執行 | 硬體事件驅動的即時修正 |
| **4** | **核心防禦項**：轉換序列規律預測電路，記錄前後區域對應關係並經重複觀測確認——具體的硬體資料結構與狀態機行為 | 硬體 pattern table + 確認機制 |
| **5** | 可配置閾值與右移衰減週期，支援依位址區間分別設定——具體的硬體暫存器配置機制 | 硬體 CSR 配置粒度 |
| **8** | 方法項通常比裝置項更易被認定為 abstract idea；防禦重點在於步驟限定「於晶片內互連中執行」，強調方法整體產生具體的技術效果（以硬體速度主動感知冷熱並產生放置建議，改善記憶體層級間的資料放置效率） | 硬體執行限定 + 技術效果 |

**建議**：若遭 §101 rejection，以 Claim 4（轉換序列規律預測電路）為核心反駁錨點，強調其硬體資料結構與狀態機行為是人腦或通用軟體無法即時執行的。必要時將 Claim 4 的限定併入獨立項以強化抗辯。Claim 3（空間策略電路）可作為備用錨點。

---

## 十、侵權判定與推廣

### 侵權判定方法

| Claim | 偵測方法 | 證據來源 |
|:---:|---------|---------|
| **1** | 確認系統是否存在設置於晶片內互連中的存取監測機制，且該機制以旁路方式運作、不影響資料路徑延遲 | 硬體技術文件、CSR register map |
| **3** | 觀察記憶體搬遷是否呈現空間擴散行為：某個 region 被 promote 後，相鄰 region 是否在短時間內也被 promote | 效能測試 |
| **4** | 觀察搬遷是否呈現序列預測行為：資料是否在被實際存取之前就被搬入近端記憶體（如 Layer N 搬遷後 Layer N+1 立即被搬入，早於實際存取） | 效能測試 |
| **5** | 確認是否存在可配置的冷熱評估參數（右移衰減週期、閾值），且支援依位址區間分別設定 | SDK 文件、driver source code |
| **7** | 確認系統是否具備近端與遠端記憶體層級，且搬遷方向與感知單元的 hint 一致 | 系統架構文件 |

**侵權判定建議路徑**：

先透過公開文件確認 Claim 5（CSR 中是否有冷熱評估相關的可配置參數），建立「該硬體具備冷熱感知機制」的事實基礎；再以效能測試觀察 Claim 4（搬遷是否呈現預測行為），佐證感知機制包含預測性策略。

### 推廣應用

| 場景 | 說明 |
|------|------|
| CXL memory tiering | 在 CXL switch 或 host 端互連中設置感知單元，管理 local DRAM 與 CXL-attached memory 間的資料放置 |
| GPU 異質記憶體 | GPU 的 HBM 與 host memory 間的資料放置管理 |
| Chiplet 多晶粒架構 | 跨 chiplet 的記憶體資料放置優化（如實施例 3） |
| 通用 NUMA 系統 | 在 NUMA 互連中設置感知單元，優化跨 node 的資料放置 |

---

*本文件為概念報告，用於對齊問題定義與解決方法。確認後將進入正式專利版本的撰寫。*

---

## Review 備忘（供正式稿參考）

以下項目已在 review 階段識別，留待正式專利稿撰寫時處理：

### Claim 結構

- 考慮新增窄獨立項：共用區域狀態表 + 冷轉熱事件觸發 + 事件驅動延伸更新，作為 Claim 1 與 Claim 4 之間的梯度防線
- Prosecution fallback 順序：寬獨立項 → 窄獨立項 → 併入 Claim 3 → 併入 Claim 4
- Claim 5 的 antecedent basis：「右移衰減週期」在 Claim 1 中無對應，考慮改依附 Claim 2
- Claim 用語統一：「請求窺探電路」（Claim）vs「旁路窺探電路」（說明書）、「存取行為追蹤電路」需在說明書中建立對應
- Claim 4「已知前兆」措辭模糊，建議改為「與已確認之前驅區域對應的冷轉熱事件」
- 方法項依附項不足（目前僅 2 個，裝置項有 6 個），需補充對應 Claim 2、3 的方法步驟版本
- 考慮新增依附項保護：雙閾值遲滯機制、搬遷抑制旗標（migration pending flag）、區域粒度可配置、CXL switch / off-chip 互連場景

### 實施例與說明書

- CXL switch 場景中的中斷通知應明確為透過 CXL.io 的 MSI/MSI-X 機制
- 閾值術語統一：「promote 閾值」「熱閾值」「提升閾值」混用，正式稿需統一
- 侵權偵測 Section 中 Claim 4 的範例（Layer N→N+1）與說明書「非位址算術規律」矛盾，應改用 MoE expert 的例子

### 前案與編輯

- 正式前案檢索時補充 NVIDIA GPU Access Counter（Volta+）作為對比
- NeoProf 正名為 NeoMem（NeoProf 為其硬體子模組名稱）
- Intel Optane 已停產且依專利撰寫原則不應使用可追溯至特定 vendor 的術語
- Banking「不與同組策略寫入衝突」斷言過度簡化，改為「可降低衝突機率」
- Section 2.2 與 3.1 高度重複，可合併
- 實施例的「可觀察行為」段落與對比表內容重複，可精簡
